# DynamicMLP (NIA29 물류 이미지 분류) — Linux 서버용 패키지 목록
#
# 설치 방법:
#   pip install -r requirements.txt
#
# PyTorch GPU 빌드는 아래 인덱스 URL을 추가로 지정해야 CUDA 지원 버전이 설치됩니다.
# GPU에 맞는 CUDA 버전을 선택하세요:
#
#   RTX 50xx (Blackwell, sm_120) — CUDA 12.8 이상 필수:
#     pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu128
#
#   RTX 30xx/40xx (Ampere/Ada) — CUDA 12.1 이상:
#     pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu121
#
#   CPU 전용 (GPU 없음/디버깅):
#     pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cpu
#
# 참고: torch/torchvision은 위 인덱스 URL에서 설치되며,
#       나머지 패키지는 PyPI에서 설치됩니다.
#       단일 인덱스로 통합 설치하려면 --extra-index-url 사용:
#         pip install -r requirements.txt \
#           --extra-index-url https://download.pytorch.org/whl/cu128

# ── Deep Learning 프레임워크 ──────────────────────────────────────────────
torch>=2.6
torchvision>=0.21

# ── 수치 연산 / 이미지 처리 ───────────────────────────────────────────────
numpy>=1.24
pillow>=9.0

# ── 평가 / 시각화 / 유틸 ──────────────────────────────────────────────────
scikit-learn>=1.3          # eval.py의 classification_report
matplotlib>=3.7            # eval.py 시각화 (주석 해제 시)
tqdm>=4.65                 # 진행률 표시
thop>=0.1                  # utils.get_flops (FLOPs/params 측정, 옵션)
